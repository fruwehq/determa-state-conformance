format: 1
namespace: conformance.case_26_unreachable
events:
  go:
    direction: input
machines:
  - machine_id: unreachable
    version: 1
    root:
      type: composite
      initial: {transition_to: a}
      states:
        a: {on_events: {go: {transition_to: b}}}
        b: {}
        orphan: {}      # nothing targets it -> unreachable
