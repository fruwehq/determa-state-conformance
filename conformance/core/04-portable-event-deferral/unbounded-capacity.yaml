format: 1
namespace: conformance.case_04_unbounded_capacity
events:
  request: { direction: input }
machines:
  - machine_id: valid
    root:
      type: composite
      initial: { transition_to: idle }
      states:
        idle:
          deferred_events: [request]
