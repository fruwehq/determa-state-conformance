format: 1
namespace: conformance.case_04_invalid_output
events:
  result: { direction: output }
machines:
  - machine_id: invalid
    root:
      type: composite
      deferred_events: [result]
      initial: { transition_to: idle }
      states:
        idle: {}
