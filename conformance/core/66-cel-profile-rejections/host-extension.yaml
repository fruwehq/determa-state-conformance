format: 1
namespace: conformance.case_66_host_extension
events:
  check:
    direction: input
machines:
  - machine_id: host_extension
    root:
      on_events:
        check:
          guard: "now() != null"
