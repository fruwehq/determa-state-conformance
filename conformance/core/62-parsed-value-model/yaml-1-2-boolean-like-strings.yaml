format: 1
namespace: conformance.case_62_yaml_1_2_boolean_like_strings
events:
  no:
    direction: input
    payload:
      value: { type: string, default: no }
  off:
    direction: input
    payload:
      value: { type: string, default: off }
  yes:
    direction: input
    payload:
      value: { type: string, default: yes }
  on:
    direction: input
    payload:
      value: { type: string, default: on }
machines:
  - machine_id: boolean_like_strings
    root:
      type: composite
      initial: { transition_to: no }
      states:
        no:
          on_events:
            no: { transition_to: off }
        off:
          on_events:
            off: { transition_to: yes }
        yes:
          on_events:
            yes: { transition_to: on }
        on:
          on_events:
            on: { transition_to: no }
