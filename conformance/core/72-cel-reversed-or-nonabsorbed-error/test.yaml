title: CEL or reports an error on either side when the other operand is false
steps:
  - send: { event: trigger }
    expect:
      status: faulted
      disposition: faulted
      emissions: []
      caller_still_owns_input: true
      fault:
        code: action_fault
        source_locator: /machines/0/root/states/running/on_events/trigger/action/0/assign/reached
